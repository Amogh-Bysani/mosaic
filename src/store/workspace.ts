import { create } from "zustand";
import { fallbackProposal,scenarioTiles } from "@/lib/layouts";
import type { CanvasMode,LayoutProposal,Scenario,Tile } from "@/lib/types";
type State={scenario:Scenario;mode:CanvasMode;tiles:Tile[];history:Tile[][];proposal?:LayoutProposal;setScenario:(s:Scenario)=>void;setMode:(m:CanvasMode)=>void;updateTile:(id:string,p:Partial<Tile>)=>void;bringForward:(id:string)=>void;focus:(id:string)=>void;applyProposal:(p?:LayoutProposal)=>void;setProposal:(p?:LayoutProposal)=>void;undo:()=>void};
export const useWorkspace=create<State>((set,get)=>({scenario:"business",mode:"personal",tiles:scenarioTiles("business"),history:[],setScenario:scenario=>set({scenario,tiles:scenarioTiles(scenario),history:[],proposal:undefined}),setMode:mode=>set({mode}),updateTile:(id,patch)=>set(s=>({tiles:s.tiles.map(t=>t.id===id?{...t,...patch}:t)})),bringForward:id=>set(s=>({tiles:s.tiles.map(t=>t.id===id?{...t,zIndex:Math.max(...s.tiles.map(x=>x.zIndex))+1}:t)})),focus:id=>set(s=>({history:[...s.history,structuredClone(s.tiles)],tiles:s.tiles.map(t=>t.id===id?{...t,x:.06,y:.06,width:.88,height:.83,zIndex:20,minimized:false}:{...t,minimized:true})})),applyProposal:p=>{const proposal=p??fallbackProposal(get().scenario,get().tiles);set(s=>({history:[...s.history,structuredClone(s.tiles)],proposal,tiles:s.tiles.map(t=>{const n=proposal.tiles.find(x=>x.id===t.id);return n?{...t,...n,minimized:false}:t})}))},setProposal:proposal=>set({proposal}),undo:()=>set(s=>{const prev=s.history.at(-1);return prev?{tiles:prev,history:s.history.slice(0,-1),proposal:undefined}:{}})}));

